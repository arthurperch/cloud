# lab/cloud — local-first AWS/Azure/DevOps lab
#
# Quick start (after packages installed):
#   cloud                  # TUI
#   cloud doctor           # CLI
#   cloud up               # LocalStack
#   cd labs/01-s3-static && terraform init && terraform apply -auto-approve
#
# Endpoint: http://localhost:4566
